<div class="container">
  <div class="row">
    <div class="col-12">
      <table class="table">
        <thead class="thead-inverse">
            <th>Name</th>
            <th>Members</th>
            <th>Actions</th>
        </thead>

        <tbody>
          <% @statics.each do |static| %>
            <tr>
                <td><%= static.name %></td>
                <td><%= static.members %></td>
                <td>
                  <%= link_to "Edit", edit_static_path(static) %>
                </td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <%= link_to "Create", new_static_path, class: "btn btn-primary" %>
      <%= link_to "Back", dashboard_index_path, class: 'btn btn-default' %>

    </div>
  </div>
</div>
